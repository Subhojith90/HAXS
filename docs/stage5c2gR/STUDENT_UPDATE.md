# Stage 5C.2G-R pre-execution update

The Stage 5C.2G pre-execution audit rejected candidate `e9a9740caac19b51a7a1da0b0f404c804d1a45d0eff45fbc602aeeee2db1c396` before production. No student calibration, untouched validation, or fixed-hole production result was generated under that candidate.

Stage 5C.2G-R repairs the decision-critical protocol defects before issuing any replacement hash. Random units are now physical and label-independent; exact and surrogate zero-coupling identities form a non-bypassable G1 gate; stochastic mobility is calibrated only from hole transport; calibration and validation use replicated occupancy/path/phase hierarchies; all local scientific source/config/test inputs are bound into a deterministic tree hash; every runner reconstructs the timestamped candidate and custody payload; calibration failure creates no usable lock; and fixed-hole checkpoints use candidate-specific hashed manifests and deterministic expected-ID merges.

The comparator remains a hard-core-bosonic constrained model. The submission does not claim exact fermionic lithium-6 dynamics. Fixed-hole inference now declares a `0.15 dB` smallest effect of scientific interest and joint uncertainty across counts and nearby times. Topology is explicitly exploratory.

After G0 passes and the replacement candidate receives an external timestamp, permission is requested for G1 paired calibration invariants only. If G1 passes, transport calibration and the replicated calibration-only benchmark may proceed sequentially. Untouched validation, fixed-hole production, Stage 5C3 production, Stage 5D, result-manuscript drafting, and public release remain blocked pending separate supervisory approval.

