# R3 to R3.1 repair ledger

| R3 finding | R3.1 control | Adversarial verification |
|---|---|---|
| Paired zero and constant-999 curves passed equality | Both independent analyzers require equality and absolute physical sanity | Canonical paired zero and constant-999 fixtures derive `absolute_sanity_passed=false` |
| Attempt-root symlink escaped evidence boundary | Evidence root and every ancestor are checked lexically and root is opened with `O_NOFOLLOW` | Canonical attempt-root symlink is rejected |
| External `PYTHONPATH` code executed | Authorization entry points require `-I`, reject import/preload variables, and G1 imports HAXS from the candidate-bound installed wheel | External shim cannot execute or pass the static gate |
| Unlisted repository-root hooks escaped identity | Explicit top-level deny-by-default preparation closure plus a fresh minimal official-G1 root | Root `conftest.py` injection is rejected; a historical development checkout cannot run official G1 |
| Negated free text satisfied receipt parsing | Exact JSON schema requires `ACCEPT_AND_AUTHORIZE_G1_ONLY`, exact hashes, exact G1 scope, and exact downstream blocks | Negated free text and non-accept decisions are rejected |
| Two virtual environments on one Mac were treated as two machines | Hashed platform UUID and serial must both differ while candidate/archive/wheel/config/plan identities agree | Two-host comparator rejects matching hardware identities |

No R3.1 scientific execution is authorized by this repair.
