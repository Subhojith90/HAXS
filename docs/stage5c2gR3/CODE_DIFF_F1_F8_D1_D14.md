# Stage 5C.2G-R3 repair ledger

| Finding | R3 closure |
|---|---|
| F1 / D3 | G1 PASS is derived from candidate-bound raw curves by two independent implementations. Stored summaries and pass flags are ignored. |
| F2 / D2 | `fcntl.flock` admits one RUNNING attempt; terminal writes require matching attempt ID and sequence. |
| F3 / D5 | Exact recursive files and directories are bound. Native extensions, loaders, `.pth`, hidden paths, symlinks, unknown files and extensionless executables are denied. |
| F4 / D7 | Manifest location is exactly candidate/config/gate/attempt scoped. Paths are relative and the recursive evidence tree must match exactly. |
| F5 / D13 | Generated egg-info was removed. Wheel build/install occurs in external temporary roots and source identity is compared before/after. |
| F6 / D14 | The broken Stage 5B0-R script is repaired; AST and compile gates cover every locked Python file. Root documentation identifies R3 and its blocks. |
| F7 / D13 | The candidate binds Python patch, OS, architecture, transitive distribution content, executable, BLAS/LAPACK, NumPy error policy and thread variables. Dependencies use a SHA-256 lock. |
| F8 / D4/D9/D12 | G2/G3 are design-only and blocked. Their top-level allocation and coverage-calibration requirements are strengthened before implementation. |
| D1 | Every scientific gate has one canonical configuration and deterministic plan hash; scientific runners accept no config/output override. |
| D6 | G/R/R2 finalization and scientific runners are rejected; R3 G2-G4 hard-exit. |
| D8 | Raw and semantic-decision files are hashed; deterministic merge protections from R2 remain candidate-bound. |
| D10 | G1 continues to use the production label-parameter mapping, now verified from raw method curves. |
| D11 | Transport remains blocked; its future design forbids squeezing inputs and increases top-level configurations. |

The rejected R2 candidate is `69347518a2103d7c5d61ca630536fa518853bc8d1e28530d29a69ce9af2a46ed`. The R3 candidate hash does not exist until the real dependency hash lock and exact-environment G0 reconstruction are completed.
