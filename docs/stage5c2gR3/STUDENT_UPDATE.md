# Stage 5C.2G-R3.1 pre-execution update

The R3 audit found six remaining authorization defects: arbitrary paired curves could pass equality, evidence-root symlinks could escape containment, external import code could run, unlisted root hooks were outside identity, free text could be misread as approval, and two virtual environments on one Mac were presented as two machines.

R3.1 adds independently implemented absolute physical-sanity predicates to both G1 analyzers; rejects symlinks at the attempt root and every ancestor; binds a deny-by-default complete preparation root; requires isolated interpreter entry points and a candidate-bound immutable installed wheel; records import origins and native-library hashes; replaces free-text approval with exact G1-only structured JSON; and requires matching G0 evidence from two physically distinct hosts identified by hashed hardware identities.

Development/retry transcripts are excluded from the review package. The supervisor bundle accepts only one named final transcript set per host, both host attestations, the two-host comparison result, the deterministic protocol archive, and its checksum.

No R3.1 scientific execution has occurred. The requested action after two-host G0 is review of the new candidate and, if accepted, issuance of a new structured receipt authorizing G1 alone. G2-G4 and every downstream claim remain blocked.
