# Stage 5C.2G-R3.2A.2 code-diff summary

This replacement candidate changes authorization and runtime custody only. The deterministic G1 scientific design is unchanged, and no receipt or scientific execution is permitted in this iteration.

## Closed defects

1. The finalizer accepts one complete candidate-bound G0 return and calls the authoritative comparator over Host-A and Host-B primary JUnit, transcript and named-test-ledger evidence. A supplied `PASS` comparator is checked against the independently recomputed object and is never trusted as authority.
2. The protocol carries an exact whole-root checksum ledger. Every file and derived directory is checked recursively; symlinks, root hooks, caches, `.pyc`, `.pyo`, `.pth`, unlisted ZIP/native/import artifacts and external import variables fail closed.
3. The official launcher completes exact CPython, dependency, wheelhouse, native-module, immutable-wheel, module-origin and whole-root checks before it creates the exclusive one-shot scientific state.
4. Authorization is one atomic object. Invalid or concurrent finalization cannot leave a usable receipt/lock pair.
5. R3.2A.1 candidate `f0f2a2e6...d4ba711` is recorded as superseded, non-executable and receipt-free.
6. The clean-root full-suite closure now binds the immutable R3.2 calibration
   failure decision and the retained R3.2A.1 compatibility wheel.  Their
   omission caused the superseded Host-A G0 candidate `46d238bd...fa8563`
   to stop with 338 passed and 2 failed tests; no scientific execution or
   authorization occurred.
7. Official G0 now marks the freshly extracted protocol tree read-only before
   execution, and retained isolated adversarial subprocesses use `-B`.  This
   closes the bytecode-write route that caused candidate
   `830d12d0...faf9ff4` to fail closed after 341 full and 108 targeted tests
   passed.  No scientific execution or authorization occurred.

## Frozen scope

- R3.2A.2 return: replacement two-host G0 only.
- Receipt and G1: forbidden pending separate supervisory acceptance.
- G2-G4, Stage 5C3/5D, result claims, exact-mobile-hole claims and public release: blocked.
