# Stage 5C.2G-R3.2A.5 Runbook

## Scope

Engineering lifecycle repair only. The deterministic G1 scientific design is unchanged. G1 and every downstream scientific stage remain blocked.

## Gate order

1. Preserve the accepted A4B Phase-A evidence and rejected A4 identity.
2. Run static compilation and the A5 lifecycle tests.
3. Run the complete and targeted retained suites.
4. Build a new wheel, environment record, ledgers, immutable-root manifest and candidate.
5. Run the fresh-unzip local complete lifecycle with a runner stub.
6. Only after all local gates pass, run replacement physical Host A and GitHub macOS ARM64 Host B G0.
7. Package the complete two-host return and stop for supervisory review.

No command may automatically issue a receipt, launch official G1, or transition to G2.
