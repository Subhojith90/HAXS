# Stage 5C Controlled Runbook

This is a controlled checkpoint only.

Run:

```bash
pytest tests/stage5c tests/stage5b1R tests/regression -q
python scripts/run_stage5c_all_lite.py
```
                                                   