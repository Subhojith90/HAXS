# Stage 5C.2G-R2 D1-D14 repair ledger

| Defect | R2 repair |
|---|---|
| D1 runtime configuration injection | One canonical path/hash per gate; runners accept no scientific config override. |
| D2 stale pass after failure | One atomic state file; RUNNING/FAILED replaces any earlier PASS. |
| D3 fabricated authorization digest | Recompute state, candidate, config, plan, attempt, raw manifest, and raw files before receipt comparison. |
| D4 missing hierarchical intervals | Nested occupancy/path/phase estimator with interval-based gate evaluation and synthetic tests. |
| D5 unexpected executable files | Exact broad runtime-file set bound to candidate; root conftest/plugin/YAML/shell additions change identity. |
| D6 rejected legacy routes runnable | Stage 5C.2G and Stage 5C.2G-R finalizers/runners hard-exit nonzero. |
| D7 incomplete output identity | Attempt-specific roots and full raw-file SHA/row/ID manifests. |
| D8 unhashed merges | Deterministic ID-sorted merge with output and ID-set hashes; duplicates/unlisted IDs rejected. |
| D9 averaged validity metrics | Interval gate evaluation for each case/label/metric. |
| D10 G1 hardcoded mapping | G1 calls the canonical production label-parameter function. |
| D11 transport loss scaling | G2 canonical config normalizes every component by its preregistered scale; G2 remains blocked. |
| D12 time covariance | Joint nested bootstrap reuses occupancy/path/phase draws across nearby times. |
| D13 environment not enforced | Python minor and exact dependency versions checked during candidate construction and runtime. |
| D14 stale documentation/broad surface | README and R2 runbook identify the current stage; all legacy entry points are visibly disabled. |

