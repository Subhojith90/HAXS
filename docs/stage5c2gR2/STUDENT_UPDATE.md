# Stage 5C.2G-R2 pre-execution update

The Stage 5C.2G-R audit confirmed the label-independent pairing repair and reproduced exact equality in an isolated canonical G1 run. It rejected candidate `e65745b3359ab2eccc12c0bd381e6d57d5aea5d1857d245e3bfd8682aaaea886` because runtime configuration, atomic gate state, supervisor authorization, exact file-set identity, raw evidence, and hierarchical validity intervals were not fail-closed.

Stage 5C.2G-R2 binds one canonical configuration and expected run plan to every gate, uses one atomic state artifact per gate/attempt, recomputes all gate and evidence digests, hashes every raw and merged output, enforces an exact runtime file set and dependency environment, disables rejected legacy routes, and implements genuine nested hierarchical intervals with synchronized nearby-time resampling.

No official G1, G2, G3, or G4 simulation has been run under R2. The next request is review of the R2 G0 package and replacement candidate. After a new external timestamp, G1 alone may run. G2-G4, Stage 5C3 production, Stage 5D, manuscript claims, exact lithium mobile-hole claims, and public release remain blocked.

